name: "Hit a webhook"
 
on:
  push:
    tags:
      - '**'
  workflow_dispatch:
 
 
jobs:
  run-updater:
    runs-on: ubuntu-latest
    steps:
    - name: REST API with curl
      run: |
         curl \
         -H "Accept: application/vnd.github+json" \
         -H "Authorization: Bearer <SiyaaJhawar>"\
         -H "X-GitHub-Api-Version: 2022-11-28" \
         https://api.publicapis.org/entries
 

   
       
         
       

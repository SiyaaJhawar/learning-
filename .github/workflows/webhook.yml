# This is a basic workflow that is manually triggered
name: Test reminder


on:
 
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  repository_dispatch:
   types:
     - webhook

     
      

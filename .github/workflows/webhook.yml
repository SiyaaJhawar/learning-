name: "Hit a webhook"
 
on:
  push:
    
  workflow_dispatch:
 
 
jobs:
  run-updater:
    runs-on: ubuntu-latest
    steps:
    - name: REST API with curl
      run: |
        curl --request GET \
        --url "https://www.boredapi.com/api/activity" \
        --header "Accept: application/vnd.github+json" \
        for (auto it = j["url"].begin(); it != j["url"].end(); ++it)
        {
               std::cout << it.key() << " | " << it.value() << "\n";
        }
         
         
        
        
       
        
        
               
            
              
           
        
         
         
        
        
        
        
        
        
         
        
 

   
       
         
       

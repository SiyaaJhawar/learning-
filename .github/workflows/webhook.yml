name: "Hit a webhook"
 
on:
  push:
    tags:
      - '**'
  workflow_dispatch:
 
 
jobs:
  run-updater:
    runs-on: ubuntu-latest
    steps:
    - name: REST API with curl
      run: |
         curl \
         -H "Accept: application/vnd.github+json" \
         -H "Authorization: Bearer <Siyaa-Jhawar>"\
         https://www.boredapi.com/api/activity
 

   
       
         
       

name: Test action

on: [ push ]

jobs:
  test-general:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: check env
        uses: SiyaaJhawar/learning-@main
        with:
          envkey_ENVIRONMENT: ${{env.ENVIRONMENT}}
          envkey_: "123456abcdef"
         
          file_name: .env
